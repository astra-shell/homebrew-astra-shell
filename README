# Astra Shell Homebrew Tap

This repository is a Homebrew tap for Astra Shell.

Install with:

```bash
brew tap astra-shell/homebrew-astra-shell
brew install astra-shell
```

The formula builds the `astra` shell version 1.0.0 from the GitHub release archive.
